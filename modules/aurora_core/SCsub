Import("env")

# -----------------------------------------------------------------------------
# Module Registration
# -----------------------------------------------------------------------------

env.add_source_files(
    env.modules_sources,
    "register_types.cpp"
)

# -----------------------------------------------------------------------------
# Bootstrap
# -----------------------------------------------------------------------------

SConscript("bootstrap/SCsub")

# -----------------------------------------------------------------------------
# Core
# -----------------------------------------------------------------------------

SConscript("core/SCsub")

# -----------------------------------------------------------------------------
# Foundation Modules
# -----------------------------------------------------------------------------

SConscript("logging/SCsub")

SConscript("memory/SCsub")

SConscript("time/SCsub")

# -----------------------------------------------------------------------------
# Core Services
# -----------------------------------------------------------------------------

SConscript("platform/SCsub")

# -----------------------------------------------------------------------------
# Events
# -----------------------------------------------------------------------------

env.Append(CPPPATH=[
    "#modules/aurora_core/events/include",
])

env.add_source_files(
    env.modules_sources,
    "events/src/*.cpp"
)

# -----------------------------------------------------------------------------
# ECS
# -----------------------------------------------------------------------------

env.Append(CPPPATH=[
    "#modules/aurora_core/ecs/include",
])

env.add_source_files(
    env.modules_sources,
    "ecs/src/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "ecs/src/storage/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "ecs/src/view/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "ecs/src/systems/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "ecs/src/commands/*.cpp"
)

# -----------------------------------------------------------------------------
# Legacy Compatibility Layer (Temporary)
# -----------------------------------------------------------------------------

env.Append(CPPPATH=[
    "#modules/aurora_core/include",
])

env.add_source_files(
    env.modules_sources,
    "src/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "src/events/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "src/thread/*.cpp"
)

env.add_source_files(
    env.modules_sources,
    "src/foundation/*.cpp"
)
